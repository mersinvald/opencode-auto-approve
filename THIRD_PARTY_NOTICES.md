# Third-party notices

The plugin code uses the MIT license in [LICENSE](LICENSE).

The Go parser imports `mvdan.cc/sh/v3` version 3.14.0. That module uses the BSD 3-Clause license. The repository preserves its notice in [shell-parser/LICENSE.mvdan-sh](shell-parser/LICENSE.mvdan-sh). The parser installer copies that notice beside the binary.

OpenCode, OpenTUI, Solid, and the other JavaScript dependencies retain their own licenses. Their pinned package versions and transitive dependencies appear in `package-lock.json`. Installed packages contain their license files.

This repository includes no model weights, session exports, personal evaluation traces, or vendor credentials.
